# Handoff: MR v4 — после PR #2, к condition 3

**Дата:** 2026-09-21. **Назначение:** дать новой сессии Claude Code точное состояние без
полагания на фоновую суммаризацию — та же причина, по которой существует
`handoff_csm_to_mean_reversion.md`. Вставьте путь к этому файлу первым сообщением новой сессии.
Не вставляйте историю чата — CLAUDE.md уже грузится автоматически и содержит всё стабильное.

## 1. Где мы находимся

**PR #2 открыт**, ветка `fix/post-only-wiring-and-test-speed`: 17 коммитов, 75 файлов,
+4908/-908. Не смёржен.

Закрыто в прошлой сессии (детали — в PR #2, не пересказывать заново, смотреть по коммитам):
- Обход emergency halt в `_process_post_only` — починен, 19 тестов, включая кросс-барный
  сценарий "уже закрытая позиция на следующем баре".
- Миграция v10 недостижима из-за порядка `executescript` — починена; негативный контроль
  подтверждён двусторонне (без `PRAGMA foreign_keys=OFF` тест краснеет — несущая; без `commit()`
  перед PRAGMA — тест не краснеет, это была страховка, не несущая часть).
- 400-барный кэп в `fetch_since` — подтверждено, что вносён только v4-рефакторингом storage;
  все прошлые числа CSM (23 079 баров) и прошлые MR signal-frequency прогоны им не затронуты.
- Коллизия LONG/SHORT на перепрыгнутой зоне выхода `[-0.5, +0.5]` — выбран вариант A (инкумбент
  побеждает), подтверждено sanity-check как immaterial для оборота на train (4 коллизии / 57
  ребалансов = 7%, total_trades не изменился).
- R7 / `csm_regime_gated` — подтверждено валидны (реальная трассировка `run_walk_forward:272`
  fallback на `settings.regime`, не только grep). Узкий, не блокирующий пробел:
  `cli.py:333` строит `Backtester` напрямую без `regime_config` — тихо берёт дефолты пайплайна.
  **Оформить отдельным issue**, не блокер.
- Ложная тревога по `db.py` CRLF — не регрессия, файл реально переписан v4-рефакторингом.

## 2. Два прямых блокера condition 3 — чинить в первую очередь

1. **`portfolio_fusion.py:146`** — `fuse()` пересобирает `PortfolioIntent` без `closes=`.
   Стоит на обоих путях пайплайна. Пока не починено — выходы стратегии ненаблюдаемы, любой
   выход атрибутируется как `rebalance`.
2. **`backtester.py:203`, `orchestrator_portfolio.py:138`** — оба читают `csm.rebalance_hours`
   независимо от активной стратегии. Реальная каденция MR сейчас 24ч, не зарегистрированные 12ч.

Оба защищены `xfail(strict=True)` в field-wired смоук-тесте — почините, тест должен упасть на
xfail, если чинится корректно (strict), а не остаться молча зелёным.

## 3. Числа этой сессии — НЕДЕЙСТВИТЕЛЬНЫ, не переиспользовать как факт

| Число | Откуда | Почему недействительно |
|---|---|---|
| 70 / 21 / 24 сделки | train, разные стадии фикса (до SL/TP снятия / после / после передачи книги) | Атрибуция закрытий (`closed_by`) была сломана `closes`-багом весь этот период |
| 4 коллизии / 57 ребалансов | train, после варианта A | Каденция ребалансировки, на которой это измерено, — 24ч, не зарегистрированные 12ч |
| n_trades = 218 | signal_frequency_check, прошлый раунд | Скрипт гонялся с `--rebalance-hours 12`, реальный код всегда использует 24 |

После починки п.2 — пересобрать все три числа заново, не корректировать старые задним числом.

## 4. Ещё открыто (не тронуто в прошлой сессии)

- 7 блокеров исходного вердикта `preregistration-guardian` (NO-GO) — **полный список не
  реконструирую здесь по памяти**, чтобы не внести ошибку через пересказ. Известны минимум три:
  заморозка документа (SHA256/commit — плейсхолдеры), честная формулировка "~90%" вместо
  "partially" по пересечению test-окна MR с уже проваленным CSM test-окном, command drift в
  `MR_implementation_status.md`. Остальные — перезапустить `preregistration-guardian` с нуля на
  обновлённом состоянии, не доверять этому списку как полному.
- **Project-level, не MR-специфично:** нет рабочего закрытия позиций в portfolio-mode живом
  оркестраторе (`orchestrator_portfolio.py:162`, нет `check_positions`) — касается и CSM, уже
  записано в `audit.md` как блокер к paper-trading. Не блокирует Task 11 (это про backtest), но
  блокирует любое движение к paper/live независимо от вердикта по MR.

## 5. Порядок действий

1. Починить `closes`-в-`fusion` (п.2.1).
2. Починить `rebalance_hours`-wiring (п.2.2).
3. Убедиться, что оба `xfail(strict=True)` кейса теперь реально проходят (flip, не остаются
   xfail) — если не flip, чинилось не то или не полностью.
4. Пересобрать числа сделок и signal-frequency заново на исправленном коде.
5. Перезапустить все три guardian-субагента (`preregistration-guardian`, `cost-gate-auditor`,
   `regression-guardian`) с нуля против нового состояния — не переиспользовать вердикты из
   прошлых сессий.
6. Свести три вердикта, доложить перед любым обсуждением готовности к Task 11.

## 6. Как использовать этот файл экономно

- Не вставлять диф PR #2 целиком — дать Claude Code самому вытащить нужное через `gh pr view 2`
  / `git log` по требованию, точнее и дешевле ручной вставки.
- Не пересказывать архитектуру — она уже в `CLAUDE.md` → `@docs/architecture/audit.md`,
  `@tasks_summary.md`, грузится автоматически при старте сессии.
- Методология (pre-registration discipline, cost-gate правила) — в `.claude/skills/
  research-methodology/SKILL.md`, подгружается сам, как только задача это потребует.
- Этот файл — единственное, что нужно вставить явно, потому что это единственное, чего нет ни
  в одном постоянном файле репозитория.
